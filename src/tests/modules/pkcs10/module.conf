pkcs10 {
        certificate_file = ${certdir}/rsa/server.pem
        private_key_file = ${certdir}/rsa/server.key
        private_key_password = 'whatever'
        pkcs10-csr = &PKCS10-CSR
        pkcs7 = &PKCS7
}
